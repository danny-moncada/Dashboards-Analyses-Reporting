A1 <- rbinom(10, 1, .2)
A1
B1 <- rbinom(10, 1, .3)
B1
t.test(A1,B1)
###############################################################################
A1 <- rbinom(100, 1, .2)

B1 <- rbinom(100, 1, .3)

t.test(A1,B1)
###############################################################################
A1<- rbinom(200, 1, .2)
B1<- rbinom(200, 1, .3)

t.test(A1,B1)
#################################################################################
A1<- rbinom(300, 1, .2)
B1<- rbinom(300, 1, .3)

t.test(A1,B1)
#It looks like at n=300 the t test can tell the difference @ 10%
#Let's see about 1% :- )

#################################################################################'
#################################################################################'
#################################################################################'
#Start with 500

A1<- rbinom(500, 1, .2)
B1<- rbinom(500, 1, .21)

t.test(A1,B1)
##################################################################################
#n = 1000

A1<- rbinom(1000, 1, .2)
B1<- rbinom(1000, 1, .21)

t.test(A1,B1)
#So far - mine cannot tell *LOL*
#Statistically that is.
#################################################################################
#n = 1500
A1<- rbinom(1500, 1, .2)
B1<- rbinom(1500, 1, .21)

t.test(A1,B1)
#So, it looks like n > 1000 it starts to be able to tell.

#We launched a power test here to check the sample size we need 
#to regularly detect a 1% difference.
power.prop.test(p1=2/10,p2=21/100,power=0.8,sig.level = 0.05)
##

